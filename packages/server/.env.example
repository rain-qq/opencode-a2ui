# Packages/server/.env overrides the workspace root .env (loaded after, wins).
# Put server-local overrides here, e.g. a different OPENCODE_BIN or OPENCODE_WORKDIR.
# Full template: see ../../.env.example
